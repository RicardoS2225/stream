import type { Channel } from './types';

// Este archivo ha sido refactorizado. 
// La lista de canales ahora se encuentra en el directorio src/lib/channels
// y se exporta desde src/lib/channels/index.ts

export const channels: Channel[] = [];
